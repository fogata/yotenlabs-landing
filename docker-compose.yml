services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yotenlabs-landing
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
